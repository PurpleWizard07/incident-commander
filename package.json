{
  "name": "incident-commander",
  "private": true,
  "version": "0.0.0",
  "description": "A WebMCP-native incident response console for the WebMCP Challenge.",
  "license": "MIT",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
